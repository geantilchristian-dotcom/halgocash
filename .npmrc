auto-install-peers=false
strict-peer-dependencies=false
# npm-style flat node_modules — fixes @types/* resolution and bin linking across workspace
node-linker=hoisted
# Use public npm registry for @replit/* packages on non-Replit environments (e.g. Render)
@replit:registry=https://registry.npmjs.org/
